(jbuild_version 1)

(executables
 ((names (main))
  (link_flags (:include link_flags))
  (libraries (mapper
              sexplib str cmdliner unix
              fmt fmt.cli fmt.tty
              logs logs.cli logs.fmt))))

(install
 ((section bin)
  (files ((main.exe as addressmapper))))
)
